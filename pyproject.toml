[project]
name = "cunhaobot"
version = "0.1.0"
description = "A multi-platform chatbot for cuñao phrases."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "python-telegram-bot<14",
    "google-cloud-datastore",
    "flask",
    "boto3",
    "google-cloud-storage",
    "pytz",
    "fuzzywuzzy",
    "pillow>=11.0.0",
    "tweepy",
]

[dependency-groups]
dev = [
    "pytest",
]

[tool.uv]
package = false
